<h1>New Band:</h1>

<%= form_with url: "/create", method: :post, local: true do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %><br>

    <br><%= form.submit "Submit" %>
  <% end %>